describe('Sudoku', function() {

  it('should test whether a row contains all numbers from 1 to 9 without repeats', function () {
    // expect(row to not have duplicates).toEqual(true);
  });

  it('should test whether a column contains all numbers from 1 to 9 without repeats', function () {
    // expect(column to not have duplicates).toEqual(true);
  });

});
